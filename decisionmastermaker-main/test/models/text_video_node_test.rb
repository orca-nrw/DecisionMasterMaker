require "test_helper"

class TextVideoNodeTest < ActiveSupport::TestCase
  #test 'TextVideoNodes do not save without content' do
  #  assert TextVideoNode.new(step_id: steps(:two).id).invalid?
  #end
end
